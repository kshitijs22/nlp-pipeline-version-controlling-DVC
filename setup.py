from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Creating versions of data and code using dvc and git',
    author='kshitij',
    license='',
)
